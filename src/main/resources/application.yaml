spring:
  application:
    name: Bank

  datasource:
    url: jdbc:mysql://localhost:3306/BankSoft
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true